node_modules
.next
out
build
dist
.env
.env.*
!.env.example
*.log
.DS_Store
coverage
playwright-report
test-results

# next.js / typescript incremental build artifacts
*.tsbuildinfo

.vercel
.env*.local

# clerk configuration (can include secrets)
/.clerk/

# local-only private files: passwords, notes, customer credit reports (PII).
# Never commit these — moved here out of the repo root on 2026-06-15.
/.private/
